# Terraform AWS EC2 with Jenkins & Docker

This project provisions an AWS EC2 instance using Terraform and automatically installs:

- Jenkins
- Docker
- Java (OpenJDK 11)

The setup uses Terraform modules and a user_data script to bootstrap the instance.

---

## 📁 Project Structure

